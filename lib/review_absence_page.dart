import 'package:flutter/material.dart';

class ReviewAbsencePage extends StatelessWidget {
  const ReviewAbsencePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Review absence page'),
    );
  }
}
